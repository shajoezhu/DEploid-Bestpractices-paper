\documentclass{article}
\usepackage{natbib,longtable,graphicx,tikz}
\usepackage{caption}
\usepackage[label font=bf,labelformat=simple, position = top, justification=RaggedRight,singlelinecheck=false]{subfig}
% \newsubfloat{figure}

\renewcommand{\thesubfigure}{\Alph{subfigure}}
\newsavebox{\measurebox}

\usepackage{fullpage, url, hyperref}
\usepackage{listings}
\lstset{frame=tb,
  language=Java,
  aboveskip=3mm,
  belowskip=3mm,
  showstringspaces=false,
  columns=flexible,
  basicstyle={\small\ttfamily},
  numbers=none,
  numberstyle=\tiny\color{gray},
  % keywordstyle=\color{blue},
  commentstyle=\color{dkgreen},
  % stringstyle=\color{mauve},
  breaklines=true,
  breakatwhitespace=true,
  tabsize=3
}
\lstset{language=bash}


\title{Supplementary Material}
\author{Sha (Joe) Zhu}
\date{}

\usepackage[label font=bf,labelformat=simple, position = top, justification=RaggedRight,singlelinecheck=false]{subfig}
\renewcommand{\thesubfigure}{\Alph{subfigure}}
\graphicspath{{./figures/}{./figures/otherFigures/}}

\setcounter{equation}{0}
\setcounter{figure}{0}
\setcounter{table}{0}
\setcounter{page}{1}
\makeatletter
\renewcommand{\thesection}{S\arabic{section}}
\renewcommand{\theequation}{S\arabic{section}.\arabic{equation}}
\renewcommand{\thefigure}{S\arabic{section}.\arabic{figure}}
\renewcommand{\thetable}{S\arabic{section}.\arabic{table}}
\renewcommand{\bibnumfmt}[1]{[S1#1]}
\renewcommand{\citenumfont}[1]{S1#1}
%%%%%%%%%% Prefix a "S" to all equations, figures, tables and reset the counter %%%%%%%%%%

%\floatsetup[figure]{style=plain,subcapbesideposition=top}
%\newsubfloat{figure}

% \usepackage{subcaption}


%%%%%%% Start on new page
\renewcommand{\thepage}{S1-\arabic{page}}
\begin{document}
\clearpage
%\setcounter{page}{1}

\maketitle
\begin{abstract}
\noindent In the supplementary material, we include background information of the data set, method details of the reference panel construction, as well as data filtering strategies, and rationale behind the choice of parameters.
\end{abstract}


\tableofcontents

%\setcounter{section}{0}

\newpage
\section{Data and reference panel}
\subsection{Pf3k data set}
% The primary output of the project will be an open access data resource with companion publications on genomic diversity and population genetics that together provide a detailed description of P. falciparum genome variation across the major malaria endemic regions.
%

The Pf3k \citep{pf3k} dataset includes 2,640 {\it Plasmodium falciparum} 


can be divided into field and lab samples based on their sources, and clonal and mixed samples based on the mixtures based on our work \citep{Zhu2017, Zhu2019}, see Table~\ref{tab:pf3k} for details. Note, we drop the duplicated samples and use only the preferred ones.

TODO: check We also exclude samples where sequencing depth is below 30 and less than 30\% of sites are callable.


% > table(meta$Type)

% Field clonal    Field mix   Lab clonal      Lab mix 
%         1365          979           86           27
\begin{table}[h]
    \centering
    \begin{tabular}{cccc}\hline\hline
                  &  Clonal samples  & Mixed samples & Total \\ \hline
    Field samples &  1365  & 979 & \\
    Lab samples &   &&\\
    Total &   &&\\
    \hline\hline
    \end{tabular}
    \caption{Field samples are collected from multiple sampling sites in Africa and Asia \citep{Duffy2015, Kamau2015, eLife2016, Mobegi2014, Amato2017, Miotto2013, Ocholla2014, Amambua-Ngwa2012, Wong2017}. Clonal and mixed field samples are identified by \citet{Zhu2019}. The lab clonal samples include 5 lab clonal samples (7G8, GB4, KH02, KE01, GN01) and 96 \textcolor{red}{check this number} crosses samples comprising parents and progeny from the {\it P. falciparum} Genetic Crosses project \citep{Miles2016}. The lab mixed samples include 27 mixed lab strains in varying proportions created by Dr Jason Wendler \citep{Wendler2015}. }
    \label{tab:pf3k}
\end{table}





\subsection{Generating {\it in silico} mixtures for deconvolution benchmark}
\subsection{{\it In silico} mixtures of lab strains}
In addition to the {\it in vitro} lab mixtures from \citet{Wendler2015}, we generated four {\it in silico} lab mixtures: We used the lab strain GB4 to provide a whole-genome read depth profile. We drew Bernoulli variables to simulate alternative allele counts, with the probability of success as the adjusted within sample allele frequency at each position. The expected WSAF is the dot product of the genotype of the lab strains: 3D7, Dd2, HB3, 7G8 and the proportions. For example, if the genotype is 0, 0, 1, 1 at an arbitrary position, and the mixture proportion of 0.1, 0.2, 0.4 and 0.3 will lead to an expected WSAF of 0.7. We then further adjust the WSAF with an error term (0.01) to account for all technical error/noise: $0.7 \times(1-0.01) + (1-0.7) \times.01 = 0.696$.

% \vspace{.5cm}
% To test the accuracy of \texttt{DEploidIBD} in a more realistic setting, we created {\it in silico} mixtures of 2, 3 and 4 strains given different transmission scenarios from mosquito bites. Let $K$ denote the number strains within each sample, and $b$ denote the number of independent mosquito bites. In such a scenario, a mixed infection containing $K$ strains can be treated as a result of $b$-bite events, where $K \in \{2,3,4\}$ and $1~{\leq}~{b}~{\leq}~K$. For example, a $K=2$ mixed infection can result from co-transmission, where two strains are passed in a single bite ($b=1$); or by superinfection, where each strain is delivered by a unique bite ($b=2$). A mixed infection containing 3 strains is more complex: besides co-infection (3 strains in a single bite, $b=1$) and superinfection (3 strains in 3 bites, $b=3$), we can also have a super-infection scenario made up from a co-infection plus a clonal-infection ($b=2$). The complete simulation procedure (code) is available at \url{https://github.com/DEploid-dev/DEploid-Data-Benchmark-in_silico_field}.

\subsection{Data quality}

\begin{figure}[ht]
% \sbox{\measurebox}{%
%   \begin{minipage}[b]{.5\textwidth}
  \subfloat
    [coverage]
    {\label{fig:figA}\includegraphics[width=.5\textwidth]{figures/sample_coverage.png}}
%   \end{minipage}}
% \sbox{\measurebox}{%
%   \begin{minipage}[b]{.5\textwidth}
  \subfloat
    [VQLSOD]
    {\label{fig:figb}\includegraphics[width=.5\textwidth]{placeholder.jpeg}}
%   \end{minipage}}

% \usebox{\measurebox}%\qquad

% \begin{minipage}[b][\ht\measurebox][s]{.62\textwidth}
% %\centering
% \subfloat
%   []
%   {\label{fig:figB}\includegraphics[width=\textwidth]{hap_viterbi_hap1.pdf}}\\
% %\vfill
% \subfloat
%   []
%   {\label{fig:figC}\includegraphics[width=\textwidth]{hap_viterbi_hap2.pdf}}
% \end{minipage}

%   \begin{subfigure}{.5\textwidth}
%   \centering
%   % include first image
%   \includegraphics[width=.8\linewidth]{placeholder.jpe}  
%   \caption{Put your sub-caption here}
%   \label{dataQ:sub-coverage}
% \end{subfigure}
% \begin{subfigure}{.5\textwidth}
%   \centering
%   % include second image
%   \includegraphics[width=.8\linewidth]{placeholder.jpe}  
%   \caption{Put your sub-caption here}
%   \label{dataQ:sub-VQSLOD}
% \end{subfigure}
% \caption{Put your caption here}
% \label{fig:dataQ}
  
\end{figure}



\subsection{Variant selection}
We ran {\tt DEploid-IBD} on high quality biallelic SNPs (both coding and non-coding, tagged with PASS at the QUAL column in the VCF file) from Pf3k~\citep{pf3k}. Before the additional filtering described below, this set contained 1,057,830 SNPs.
\linespread{1}
\begin{lstlisting}
bcftools view \
--include 'FILTER="PASS"' \
--min-alleles 2 \
--max-alleles 2 \
--types snps \
--output-file SNP_INDEL_Pf3D7_01_v3.high_quality_biallelic_snps.vcf.gz \
--output-type z \
SNP_INDEL_Pf3D7_01_v3.combined.filtered.vcf.gz
\end{lstlisting}


\subsection{Variant quality score log-odds}

The variant quality score log-odds (VQSLOD) score measures how reliable each variant call is. For the Pf3k data set, all `PASS' sites have VQSLOD score above zero.

% provides a continuous estimate of the probability that each variant is true, allowing one to partition the call sets into quality tranches. The main purpose of the tranches is to establish thresholds within your

get a table

chromsomes, by row,, col



\subsection{Data cleaning and filtering}

\subsubsection{High leverage data points}
Same as \citet{Zhu2017,Zhu2019}, we find markers with high frequencies in both reference and alternative allele count can mislead our model to fit extra strains. We used a threshold of $\geq$ 99.5\% coverage (default) to identify markers with extremely high allele counts. We further expand this list of potential ``bad markers'' by considering their nearest 10 neighbours on both sides along the genome, and identify the nearest markers as ``to be excluded'' if overlaps are found (see Figure~\ref{fig:fitering}). These poorly-genotyped variants are likely to be artefacts of mapping and genotype calling.

\begin{figure}[htp]
  \centering
    \includegraphics[width=0.7\textwidth]{{PG0415-CaltVsRefAndWSAFvsPLAF}.png}
  \caption{Figure on the top plot total allele counts across all markers. We observe a small number of heterozygous sites with high coverage (marked as crosses above), which can potentially mislead our model to over-fit the data with additional strains.  We use a threshold of $\geq$ 99.5\% coverage to identify markers with extremely high allele counts. Red crosses indicate markers that are filtered out. Figure on the left plots the alternative allele count against the reference allele count. As {\it P. falciparum} genomes are haploid, in clonal samples, one would expect to see either alternative or reference allele at any sites. Heterozygous sites are indications of mixed infection. The marked black crosses can mislead our model to overestimate the number of strains as two. Figure in the middle is the histogram of the allele frequency within sample. Figure on the right show allele frequency within sample, compare against the population average.
  }
  \label{fig:fitering}
\end{figure}

This process remains as a manual step, as it may consider other sites.

\subsection{Reference panel}

\begin{figure}[ht]
  \centering
    \includegraphics[width=0.7\textwidth]{placeholder.jpeg}
    \caption{PCA/tSNE/UMAP of the big panel}
\end{figure}




\newpage
\renewcommand{\thepage}{S2--\arabic{page}}

\setcounter{page}{1}


\section{DEploid-LASSO deep dive}
\subsection{Model}
Assume Independence 

\subsection{Effective reference panel}

\newpage
\renewcommand{\thepage}{S3--\arabic{page}}

\setcounter{page}{1}

\section{Major improvement from DEploid and DEploid-IBD}
\subsection{Optimised reference panel per sample}


\subsection{Reduced run time}
\begin{figure}{ht}
\centering
\includegraphics[width=.7\textwidth]{figures/placeholder.jpeg}
\caption{bar plot of time, four bars, no panel, with panel, ibd, best practice}
\end{figure}

\subsection{Improved inference for balanced mixtures}

\subsection{Improved inference results for mixtures of 3- or 4-strains}

\newpage
\renewcommand{\thepage}{S4--\arabic{page}}

\setcounter{page}{1}
\section{Github actions}

\newpage
\renewcommand{\thepage}{S5--\arabic{page}}

\setcounter{page}{1}
\section{Modulerized package development}

\subsection{}



\newpage

\renewcommand{\thepage}{S6--\arabic{page}}

\setcounter{page}{1}
\section{Deconvolution of the field samples}
Different than before, we are going for whole genome

\begin{figure}[ht]
  \centering
    \includegraphics[width=0.7\textwidth]{placeholder.jpeg}
  \caption{PCA}
\end{figure}

\newpage



% \FloatBarrier
\bibliographystyle{chicagoa}
\bibliography{dbp.bib}

\end{document}
